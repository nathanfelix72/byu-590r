; Raise upload limits for avatar and book cover uploads (default 2MB is too small).
; Used when the web server document root is backend/public (Apache/Nginx + PHP-FPM).
upload_max_filesize = 10M
post_max_size = 12M
